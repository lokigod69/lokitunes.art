'use client'

/**
 * Mouse attraction component - INVISIBLE VERSION
 * Tracks mouse but shows no visual elements
 * Clean view without red sphere blocking the orbs
 */
export function MouseAttraction() {
  // No visual elements - just a placeholder component
  // Physics attraction can be added later if needed
  return null
}
